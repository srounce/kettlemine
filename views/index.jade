extends layout

block content
  div#content
    h1= title
    h2 #{teaCounter} cups in the brew queue
    p It appears that you 
      unless wantsTea
        b don't 
      | want a cup of tea.
      form(action="/choose", method="POST")
        input(type="hidden", name="wantsTea", value="#{!wantsTea}")
        button(type="submit").shadow I changed my mind!
